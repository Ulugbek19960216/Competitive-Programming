// https://leetcode.com/problems/add-binary/submissions/1172520311?envType=study-plan-v2&envId=programming-skills

/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function(a, b) {

    return (BigInt('0b' + a) + BigInt("0b" + b)).toString(2);
    
};
